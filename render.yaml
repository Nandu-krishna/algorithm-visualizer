services:
  - type: web
    name: algorithm-visualizer-api
    runtime: java
    buildCommand: cd backend && mvn clean package -DskipTests
    startCommand: cd backend && java -jar target/algorithm-visualizer-0.0.1-SNAPSHOT.jar
    plan: free
